<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body class="text-light bg-dark">
  
  <%- include('partials/_header') %>
  
  <main style="margin: 1em;" class="bg-dark">
    <h1>My URLs</h1>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Short URL</th>
          <th scope="col">Long URL</th>
          <th scope="col">Edit</th>
          <th scope="col">Delete</th>
        </tr>
      </thead>
      <tbody>
        <% if (user) { %>
          <% for (let url in urls) { %>
              <tr>
                <td><%= url %></td>
                <td><%= urls[url].longURL %></td>
                <form method="GET" action="/urls/<%= url %>">
                  <td><button type="submit" class="btn btn-primary">Edit</button></td>
                </form>
                <form method="POST" action="/urls/<%= url %>?_method=DELETE">
                  <td><button type="submit" class="btn btn-danger">Delete</button></td>
                </form>
              </tr>
          <% } %>
        <% } else { %>
          <p style="font-size: 1.5em; color: red">Please log in or register to view your URLs</p>
        <% } %>
      </tbody>
    </table>
  </main>
  
  <%- include('partials/_scripts') %>
</body>
</html>
